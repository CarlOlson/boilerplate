{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "watch": "nodemon -q -w src -w test --exec \"npm run -s test\"",
    "test": "npm run -s mocha && npm run -s stryker",
    "mocha": "nyc mocha",
    "stryker": "stryker run stryker.conf.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "nodemon": "^1.11.0",
    "nyc": "^10.1.2",
    "stryker": "^0.5.7",
    "stryker-api": "^0.4.2",
    "stryker-mocha-runner": "^0.2.0"
  }
}
